// 20. Using slice, write a JavaScript function to clone an array
let arrNumb = [1, 4, 56, 6, 2]
function  colneArrayToSlice(arr) {

   let result = arr.slice()
   return result
}

console.log(colneArrayToSlice(arrNumb))